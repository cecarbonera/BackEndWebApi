{
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Capacitacao": "Data Source=LAPTOP-9USBKHC5\\LOCAL;Initial Catalog=Capacitacao; Integrated Security=true",
    "MongoDB": "mongodb://localhost:27017/Logs"
  },
  "Logging": {
    "LogLevel": { // All providers, LogLevel applies to all the enabled providers.
      "Default": "Error", // Default logging, Error and higher.
      "Microsoft": "Error" // All Microsoft* categories, Warning and higher.
    },
    "Debug": { // Debug provider.
      "LogLevel": {
        "Default": "Error", // Overrides preceding LogLevel:Default setting.
        "Microsoft.Hosting": "Error" // Debug:Microsoft.Hosting category.
      }
    },
    "EventSource": { // EventSource provider
      "LogLevel": {
        "Default": "Error" // All categories of EventSource provider.
      }
    }
  }
}